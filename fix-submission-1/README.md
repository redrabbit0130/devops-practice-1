# Fix Submission 1

This is a simple Node.js app for DevOps practice.

The app runs on port `3000`.

## Project Structure

```text
fix-submission-1/
  server.js
  package.json
  Dockerfile
  docker-compose.yml
  .dockerignore
  README.md
